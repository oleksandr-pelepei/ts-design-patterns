import {expect} from 'chai';

describe('Test', () => {
    it('1 plus one shoud be equal 2', () => {
        expect(1 + 1).to.be.equal(2);
    });

    it('Two plus one shoud be equal three', () => {
        expect(1 + 1).to.be.equal(3);
    });
});
